diff --git a/app/config.py b/app/config.py
index 1111111..2222222 100644
--- a/app/config.py
+++ b/app/config.py
@@ -8,6 +8,8 @@ import os

 class Settings:
     database_url: str = os.environ["DATABASE_URL"]
+    redis_url: str = os.environ["REDIS_URL"]
+    cache_ttl: int = int(os.getenv("CACHE_TTL_SECONDS", "300"))
     debug: bool = False
diff --git a/app/api.py b/app/api.py
index 3333333..4444444 100644
--- a/app/api.py
+++ b/app/api.py
@@ -14,6 +14,11 @@ router = APIRouter()
 async def get_widget(widget_id: int):
     return await store.load(widget_id)

+
+@router.post("/widgets/{widget_id}/refresh")
+async def refresh_widget(widget_id: int):
+    return await store.refresh(widget_id)
+
diff --git a/requirements.txt b/requirements.txt
index 5555555..6666666 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -1,3 +1,4 @@
 fastapi==0.115.0
 uvicorn==0.32.0
+redis==5.2.0
 httpx==0.27.2
diff --git a/app/utils.py b/app/utils.py
index 7777777..8888888 100644
--- a/app/utils.py
+++ b/app/utils.py
@@ -1,4 +1,4 @@
-  def add(a, b):
-      return a + b
+    def add(a, b):
+        return a + b
diff --git a/poetry.lock b/poetry.lock
index 9999999..aaaaaaa 100644
--- a/poetry.lock
+++ b/poetry.lock
@@ -120,6 +120,14 @@ files = [
+[[package]]
+name = "redis"
+version = "5.2.0"
+description = "Python client for Redis"
diff --git a/Dockerfile b/Dockerfile
index bbbbbbb..ccccccc 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -5,5 +5,6 @@ WORKDIR /srv
 RUN pip install -r requirements.txt
-EXPOSE 8000
-CMD ["uvicorn", "app.main:app", "--port", "8000"]
+EXPOSE 9000
+CMD ["uvicorn", "app.main:app", "--port", "9000"]
